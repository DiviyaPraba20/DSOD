import { AppState } from './app.state';
import { AuthState } from '../pages/auth/states/auth.state';
import { CMSState } from '../cms/states/cms.state';

export const states = [AppState, AuthState, CMSState];
